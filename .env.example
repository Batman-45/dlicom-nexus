# Set to true to use deterministic offline mock mode (zero X API requests, 100% free)
VITE_USE_MOCK_DATA=true

# Optional local backend proxy URL (only used when VITE_USE_MOCK_DATA=false)
VITE_API_PROXY_URL=http://localhost:3000
