# Dlicom Circle Environment Configuration
# Production on Vercel serves the Serverless API from the same origin (/api/...)
# VITE_API_PROXY_URL is NOT required in production.
# For local dev with a standalone proxy, you can optionally set VITE_API_PROXY_URL=http://localhost:3000

VITE_USE_MOCK_DATA=false
